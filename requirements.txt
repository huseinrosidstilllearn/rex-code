# Config & validation
pydantic==2.13.5
python-dotenv==1.2.3

# Optional voice input (CLI mic capture)
# sounddevice==0.5.1
# numpy==2.2.3

# Optional local Whisper (offline, heavy)
# faster-whisper==1.1.1

# LLM Providers
google-genai==2.22.0
openai==3.8.0
httpx==0.28.1

# Native TUI (Claude-Code-style terminal app)
textual==0.86.3

# IANA timezone database (zoneinfo) — required on Windows, optional elsewhere
tzdata==2026.3
